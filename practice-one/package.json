{
    "scripts": {
        "start": "cd src && npx parcel index.html",
        "format": "prettier --write ."
    },
    "devDependencies": {
        "parcel": "^2.12.0",
        "prettier": "^3.3.3"
    }
}
